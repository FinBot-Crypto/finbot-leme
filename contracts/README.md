# Contrato de integração

O Leme publica somente payloads compatíveis com `trade.order.v1` no subject `trade.order.v1`. O `finbot-core` é o consumidor executor. Esta cópia do contrato é versionada junto ao Leme para permitir validação local sem depender do checkout do Core.

